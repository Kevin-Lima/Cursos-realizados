//direct export
export const key = {
    value: "2313jklfds",
    permission: "admin",
}

//nos temos que guardar a função dentro de uma constante para que esse tipo de export funcione
export const getDataFromApi = () => {
    console.log("dados da api sendo buscados...")
}