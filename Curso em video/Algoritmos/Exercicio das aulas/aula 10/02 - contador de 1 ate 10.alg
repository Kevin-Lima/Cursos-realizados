algoritmo "contador de 1 ate 10"
var
   contador: inteiro
inicio
      contador <- 1

      Repita
            Escreval(contador)
            contador <- contador + 1
      Ate (contador > 10)

fimalgoritmo
